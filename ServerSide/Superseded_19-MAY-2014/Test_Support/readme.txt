[to do]
sessionhandler
	Stored Procs
	Verify session exists at particular stage, and update stage
	>>verify session times, greater than 1hr = drop session

globeHandler
	search db for globe, and permissions level
	insert new globe
	update globe
	assign globe

fileHandler
	generate unique folder structure
	initialize garbage collector to remove after period expires
	create copy of files in folder structure, with unique names
	return structure to client
	
	
